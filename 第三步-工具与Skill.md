# 第三步：工具与 Skill

**状态：9 个模型工具、2 份 Skill 及强制 TODO 机制已认可，作为后续实现依据。** 覆盖同 SKU 换货／补发及后续进度查询；下一步见 [权限与安全边界](第四步-权限与安全边界.md)。当前尚未编写接口代码或 Skill 正文。

**第五步补充设计：** 为读取外置的临时上下文，拟增加第 10 个工具 `read_context(path, cursor?, query?)`，仅能分片读取当前任务已登记文件；写入和清理由运行时负责。原有 9 个工具不变，具体契约见 [上下文管理](第五步-上下文管理.md)。

## 1. 四种职责先分清

| 内容 | 回答什么问题 | 例子 |
|---|---|---|
| Tool | 能执行哪一个有明确输入输出的操作？ | 查询订单、保存草案 |
| Skill | 遇到这类问题，怎样组合能力、追问和解释？ | 破损商品处理指导 |
| RAG 知识 | 当前适用的政策依据是什么？ | 对应渠道和版本的材料要求 |
| Plugin | 能力由谁提供，如何装配和释放资源？ | 一个售后插件注册多个工具 |

工具不需要一一对应插件；Skill 也不需要一一对应工具。运行时的身份、确认和幂等检查始终由程序执行，不能靠是否加载了某份 Skill 才生效。

## 2. 模型可调用的 9 个工具

下面仅列主要参数，身份与当前任务由服务端注入，模型不传 `user_id`、批准状态或权限。

| 工具 | 主要输入 → 输出 | 解决的问题与边界 |
|---|---|---|
| `list_orders` | 商品／日期线索、分页 → 当前用户订单摘要 | 定位“上次买的那瓶”；范围和结果数受限 |
| `get_order` | `order_id` → 订单行、SKU、数量、履约事实 | 读取选定订单；模型不能用摘要猜详情 |
| `search_policies` | `query`，可选 `order_id` → 条款片段、引用 ID、版本 | 基于订单确定渠道与适用范围；范围不清则说明缺项，不能跨渠道随意套用 |
| `get_replacement_options` | `order_id / line_id / quantity` → 同 SKU 库存、履约选项及可证实的预计时间 | 查是否有可选方案；不是承诺或库存预留，执行前重查 |
| `get_service_status` | `order_id` → 关联售后申请、当前状态与已记录事件 | 回答“补发到哪了”，也帮助发现已有申请；无记录不等于历史上未操作 |
| `propose_replacement` | 订单行、数量、原因、引用／材料 ID → 草案 ID、版本、确认摘要或缺项 | 只保存可供确认的方案，不创建实际补发单；SKU 与收货信息由订单解析 |
| `request_handoff` | 原因、相关证据 ID → 本地人工待办 ID 与状态 | 能力不足或无法核验时保留处理上下文；按任务去重，不自动发外部消息 |
| `load_skill` | 允许列表中的名称 → 指导正文及版本标识 | 按需加载业务指导，不能加载任意路径或远程代码 |
| `todo` | `mode: update/review`、预期版本、计划条目 → 当前计划及核对记录 | 开始前制定计划，处理过程中更新或明确复核；不修改用户目标、确认状态或业务回执 |

前五个只查询，接着两个改变草案／待办状态，最后两个加载指导、管理计划。`list_orders` 与 `get_order` 分别处理定位和详情；`get_service_status` 读取售后流程，不把所有状态都塞进订单工具。

用户直接提供订单号时，可跳过列表查询，但仍必须核对归属。政策搜索的商家、渠道和时间约束由可信上下文解析，工具返回的证据不是执行授权。数量、材料引用、草案及待办都由服务端校验，重复提出相同草案不重复创建。

## 3. 有些能力不交给模型

批准草案、创建实际补发单、按操作号对账、重试调度和标记任务完成，都是**运行时内部能力**。确认事件携带草案 ID 与版本，程序读取已保存参数、重新检查条件后执行；模型无法调用一个“把自己批准了”的工具。

首版也不提供通用 SQL、Shell、任意 HTTP 请求或直接操作 Redis／Milvus 的工具。数据库由后端适配器访问。追问通过普通回复完成，卡片根据结构化结果渲染，暂不增加 `ask_user` 或专用展示工具。

工具统一返回 `status / data / evidence_refs / error`；业务数据附必要的查询时间与版本。无库存是业务结果，网络失败是技术错误；重试策略由执行器决定。实际写入结果未知的处理继续沿用第二步的对账机制。

### TODO：先计划，定期核对，由运行时强制

- **初次进入模型循环先建计划。** 没有计划时先调用 `todo(update)`，通常列出 3～5 个结果明确的步骤，再允许业务查询或提出草案。条目包含稳定 ID、说明、`pending / in_progress / blocked / completed` 和相关证据引用；最多一个条目处于进行中。
- **每 3 个未成功调用 TODO 的模型决策步强制核对。** 这里按第二步定义的 Step 计数，包含普通回复，跨用户轮次累计；不是等三条用户消息。下一次模型请求强制调用 `todo`，成功后恢复普通工具。阈值可配置。
- **核对可以不改计划。** `review` 明确确认当前目标、进度和未完成项；没有计划时不能只 review。只有成功且版本匹配的更新／复核才清零计数，参数错误或只在文字里提到 TODO 都不算。
- **防止遗忘，也防止改写目标。** 每步保留诉求与约束要点和当前计划；超长原文按第五步外置并保留定位，当前决策涉及的条件需读回核对。TODO 不能改任务目标；用户改变诉求后，运行时登记目标变更并要求重新计划。业务完成项需关联可核验结果，勾完 TODO 不等于任务完成。
- **不增加计划服务。** 计划放入现有任务快照，复核写入事件记录；重启恢复版本与计数。计划管理作为工具插件提供，强制触发由循环负责，Skill 只提供拆解指导。

强制核对也是模型调用，计入原有步数、token 与超时预算；不延长总预算。模型不遵守工具约束时不执行其他调用，预算内仅允许一次纠正，仍失败则暂停。等待中的任务不为刷新 TODO 自行唤醒；确认执行、结果对账等已确定的运行时路径仍直接处理，无需额外模型规划。

## 4. 两份 Skill，各管一个阶段

| Skill | 什么时候使用 | 内容重点 |
|---|---|---|
| `damage-replacement` | 商品破损、泵头异常，需要新建换货／补发方案 | 复用已提供信息；定位订单行；查询政策与库存；只追问缺项；给出可确认的方案 |
| `service-followup` | 已有售后申请，用户询问状态、进度或催促 | 区分申请、受理、发出、签收；说明记录支持的预计时间；无法解释或超出能力时登记人工待办 |

例如，第一份 Skill 的指导可以是：先简短回应用户遇到的具体问题；查明订单与政策；缺材料时一次说明缺什么；库存不足就解释现有选项；已有申请时先核验，避免重复提出补发。

**不写进 Skill：** 固定退换货天数、具体补偿金额、实时库存，以及批准／重试状态机。这些分别属于政策知识、业务事实和程序规则。共情表达放在上述处理指导中，不另设“共情 Agent”或“共情 Skill”。

两份 Skill 以 `SKILL.md` 保存 `name / description` 和正文，首版不带脚本。系统提示词保留短索引，模型通过 `load_skill` 读取所需正文；任务记录已加载版本，恢复时由上下文构建器按需重新提供，避免每轮重复加载。

这种组织方式参考 [Commerce Agents 的索引与加载器][skills]及 [Agent Skills 的渐进加载规范][spec]。两份短 Skill 不一定比直接常驻更省 token；后续比较加载开销与任务效果，不先声称成本下降。Skill 缺失时报告配置问题，关键执行检查照常生效。

## 5. 两个需要接住的体验细节

**材料从哪里来？** 通过前端上传入口或受信业务记录产生材料 ID，模型只引用，不能把文件名当成图片内容。赛事图片实际缺失，首版没有视觉验证能力时进入人工核验，不虚构“已看图确认破损”。上传与材料状态属于应用能力，本轮不扩展成多模态模块。

**草案提出后用户改口怎么办？** 普通新输入在运行时使受影响的旧草案失去执行资格；首版不支持改地址或跨 SKU，更改请求转人工。已发出的操作先核验结果。无需再给模型一组撤回、批准和重发工具。

人工接续落地为现有界面中的一个待办详情与处理入口。登记成功只能说“已登记，等待人工处理”；只有工作人员实际认领，才能显示“已接入”。不承诺首版已有真实客服团队。

## 6. 串起来看一次

```text
“泵头坏了，想换同色号”
  todo(update：核对订单与政策、确认可用方案、提交并核验)
  load_skill(damage-replacement)
  list_orders → get_order
  连续 3 个模型决策步未复核：运行时强制 todo(review/update)
  search_policies → get_service_status → get_replacement_options
  信息不足：追问并等待；信息充分：propose_replacement
  用户确认 → 运行时校验并执行 → 展示实际回执

“刚才的补发到哪了？”
  load_skill(service-followup) → get_service_status
  用当前记录解释；需要人工处理时 request_handoff
```

这是示例调用路径，不是每轮必须完整执行的固定脚本；强制 TODO 的实际位置按决策步计数，不按上面的工具箭头计数。已有有效信息可复用，判断写入条件时仍查当前事实。

**本步已认可清单：9 个工具与 2 份 Skill；第五步拟补充 `read_context`。** 除业务边界外，验证先计划后调用、第三步后强制复核、跨轮／重启计数保留，以及 TODO 不能伪造业务完成。另比较“无 TODO／仅自愿调用／定期强制复核”的遗漏率与额外成本，验证它是否有效。

### 参考与实际借鉴

- [Commerce Agents 工具注册表][registry]：单工具描述职责，跨工具指导放入提示词或 Skill；借鉴参数契约与配置允许列表。
- [Commerce Agents customer-care][care]：借鉴“订单事实、政策依据、下一步”的处理指导。原 Skill 的售后流程只读；本项目的草案和受控执行是新增设计。
- [S1：Commerce Agents SkillRegistry][skills]：借鉴索引常驻、正文按需读取。
- [S2：Agent Skills 规范][spec]：采用基础文件格式和渐进加载；不将文档中的工具声明视为宿主强制授权。
- [DSH 工具执行层][dsh-tools]：借鉴统一注册与执行检查；插件提供能力，强制检查仍由执行路径保证。
- [Commerce Agents 的强制工具选择][grounding-loop]：借鉴由宿主设置 `tool_choice` 的机制；周期性 TODO 是本项目新增规则，3 步是待评测的初始阈值，并非仓库结论。

[registry]: https://github.com/anthropics/commerce-agents/blob/fd4d59224ab96b43c6dc6888207c67b3bd5a24cf/shopping-agent/core/shopping_agent/tools/registry.py
[care]: https://github.com/anthropics/commerce-agents/blob/fd4d59224ab96b43c6dc6888207c67b3bd5a24cf/shopping-agent/skills/customer-care/SKILL.md
[skills]: https://github.com/anthropics/commerce-agents/blob/fd4d59224ab96b43c6dc6888207c67b3bd5a24cf/commerce-common/commerce_common/skills.py
[spec]: https://agentskills.io/specification
[dsh-tools]: https://github.com/deepseek-ai/deepseek-harness/blob/5dda764ed3aa172535a7967b06ff95d9cbfe536a/packages/core/tools/src/index.ts
[grounding-loop]: https://github.com/anthropics/commerce-agents/blob/fd4d59224ab96b43c6dc6888207c67b3bd5a24cf/shopping-agent/runtime-messages-api/shopping_agent_runtime/orchestrator.py
